-- Optional initial SQL for Superset Postgres container
-- This file is executed only on first container initialization.
-- It is safe to keep; you can add DB setup SQL here.

-- Example: ensure the "uuid-ossp" extension (commented out by default)
-- CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- No-op placeholder
SELECT 1;